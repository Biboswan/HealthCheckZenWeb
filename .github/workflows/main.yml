name: Build and Deploy
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - name: deploy to server
        uses: appleboy/ssh-action@v0.0.9
        env:
          MYPASSPHARASE: ${{secrets.MYPASSPHARASE}}
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: |
            cd ui/HealthCheckZenWeb
            echo $MYPASSPHARASE | git pull origin master
            yarn
            yarn build
